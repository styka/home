# 115 — mapowanie kryteriów akceptacji na dowody (input do `/verify`)

Stan na koniec fazy implement (T-1…T-21 ✅). „Dowód" = plik/mechanizm, który realizuje kryterium,
plus bramka/test, który go pilnuje. Pełny `npm run build` i `test:unit` (1483/1483) zielone na
lokalnym Postgresie (T-20).

| AC | Dowód w kodzie | Weryfikacja |
|---|---|---|
| AC-1 | `specs/115-integracje-modulow/analiza.md` — rozdz. 4 (macierz par ze statusem i uzasadnieniem dla każdej pary), rozdz. 5 (odłożone z powodami), rozdz. 6 (zlecenia Z-INT-01…19 z pełnymi polami) | przegląd dokumentu; raport seedowany migracją 0283 |
| AC-2 | `health/actions/health.ts` — `cost` w create/update (migr. 0280) + `bookHealthEventCost(id)`; UI `HealthHomePage` (przycisk Wallet + komunikat, także o `brak-konta`) | idempotencja: `bookAutoExpense` upsert po (`sourceModule`,`sourceId`) z `force` — korekta, nie dubel; tsc/lint/manifest |
| AC-3 | `pets/actions/petCare.ts` `bookVetVisitCost` (wydatek), `petBreeding.ts` `bookSaleIncome` (`kind:"income"`, sourceId `sale-<id>`) | jw. — jeden `sourceId` na źródło; UI w `PetSections`/`PetBreeding` |
| AC-4 | `warsztaty/actions/warsztat.ts` `addWorkshopLowStockToShoppingList(listId)` — deficyt do progu (`max(min−qty,0)||min`), przez `addItemStructured` z kontraktu Zakupów; UI `LowStockDoZakupow` w agendzie przeglądów | wzorzec zmierzony na `addLowStockToShoppingList` Magazynowania; guard listy w `addItemStructured` |
| AC-5 | `WorkshopProjectInput.cost` (migr. 0281) + `bookProjectCost(id)` (sourceId `projekt-<id>`); UI w `WorkshopDetail` ProjectsTab | idempotencja jw. |
| AC-6 | helper `src/lib/kontaktZWpisu.ts` (`zapiszKontaktZWpisu` → `{utworzono, istnial}`, dedup po nazwie, locale pl-PL) + `saveDoctorToContacts` (tag „lekarz"), `saveVetToContacts` (tag „weterynarz"), `saveProviderToContacts` (tag „wykonawca", notatka z linkiem profilu) | test jednostkowy `kontaktZWpisu.test.ts` (dedup); trzy przyciski UI z komunikatem „istniał" |
| AC-7 | `calendar/actions/doZadan.ts` `dodajPozycjeDoZadan` (termin z pozycji, odnośnik w opisie), `contacts:createTaskFromContact`, `notes:createTaskFromNote`, `czat:zadanieZWiadomosci` (link `/czat?r=<conv>#w-<msg>`) — wszystkie z pre-checkiem `hasPermission(tasksModule.permission)` i przez `createTask` z kontraktu Zadań | przyciski w CalendarPage / ContactRow / NoteRow / WatekRozmowy ze stanem ✓/błąd |
| AC-8 | `news:saveItemAsNote` (streszczenie + źródło + URL, markdown; guard własności jak `acknowledgeItem`), `youtube:zapiszFilmJakoNotatke` (streszczenie z `AiContent` BEZ generacji, fallback opis, + kanał + adres) | oba przez `createNote` z kontraktu Notatek + pre-check `notesModule.permission`; UI: stopka NewsItemCard, sekcja FilmSzczegol |
| AC-9 | sekcja „Fiszki z filmu" w `FilmSzczegol` — tylko przy transkrypcji i istniejących taliach; `getDecks` → `/api/llm/languages/extract` (transkrypcja ≤12k, języki z talii) → checkboxy → `bulkAddWords`; `AiCostBadge akcja="Fiszki z filmu"` | operacja wyłącznie na klik (on-demand), koszt z `res.usage`; brak nowej akcji serwera |
| AC-10 | flota contract += `computeConsumption`/`avgFuelPrice` (test w `flota.test.ts`); truck: select pojazdu (`getVehicles`), metryka kosztu z komunikatem o brakach danych, `zaksiegujKosztTrasy` (sourceId = sha1(start\|cel\|dzień)) | test `avgFuelPrice` zielony; idempotencja per trasa/dzień |
| AC-11 | `WeatherPref.kalendarzPrognoza` (migr. 0282, default true) + `getKalendarzPrognoza`/`setKalendarzPrognoza`; kontrakt Pogody; strona kalendarza dokłada `prognoza` przy `module.weather`; komórka siatki: emoji+tMax; przełącznik w `WatchersPanel` (odchylenie od planu odnotowane w plan.md — Pogoda nie ma widoku ustawień) | wyłączenie ⇒ `{wlaczona:false, dni:[]}` ⇒ render warunkowy znika bez zmiany logiki siatki |
| AC-12 | `completeShopping(opts.doSpizarni)` — pozycje DONE → `addPantryItem` z kontraktu Kuchni PO transakcji (błąd nie cofa zakończenia; wynik `dodanoDoSpizarni`); checkbox w `CompleteShoppingModal` (widoczny przy `module.kitchen`, domyślnie ODZNACZONY, localStorage `wom_shopping_pantry`) | serwerowy pre-check `kitchenModule.permission`; kupione bez opcji nie trafiają do spiżarni |
| AC-13 | 4 wkłady `dashboard.ts` (habits/warsztaty/contacts/weather z `Promise.race` 3 s), pola w `DashboardSnapshot`+`EMPTY_SNAPSHOT`, wpisy `DASHBOARD_CONTRIBUTORS`, render w sekcjach `today` (kolumny Warsztat/Urodziny) i `modules` (kafle Nawyki N/M, braki, pogoda) — sekcje `today`/`modules` już podlegają personalizacji (`DashboardPref`), więc nowe dane dziedziczą ją bez nowych kluczy | `check:module-registry` zielony (obustronne wpięcie); próg N+1 pulpitu 11→13 z notą (powtórzenia = 1) |
| AC-14 | magazyn contract += `getStorageItems`; strona `/rosliny/ewidencja` podaje pozycje tylko przy `module.magazynowanie` (try/catch → pusta lista); `Ewidencja`: select środka (nazwa wypełnia puste pole „środek") + ilość; po `recordTreatment` → `adjustStorageQuantity(−ilość, "wydanie", "ewidencja zabiegu <data>")` osobno od zapisu — błąd stanu tylko dopowiada w komunikacie, wpis zostaje | bez wyboru środka przepływ niezmieniony; ruch magazynowy niesie opis źródła |
| AC-15 | migracja `0283_raport_integracje_115` — `INSERT … ON CONFLICT ("slug") DO NOTHING`, slug `integracje-miedzymodulowe-115`, treść = pełna analiza | zastosowana lokalnie (`migrate deploy`), wiersz zweryfikowany zapytaniem |
| AC-16 | wszystkie 19 zleceń idzie przez Server Actions modułu źródłowego + kontrakty modułów docelowych (`bookAutoExpense`, `createTask`, `createNote`, `addItemStructured`, `addPantryItem`, `bulkAddWords`, `adjustStorageQuantity`, `getKalendarzPrognoza`); zero bezpośrednich zapisów do cudzych tabel | `check:boundaries` + pełny `npm run build` (wszystkie bramki) + `test:unit` 1483/1483 zielone (T-20) |

Odchylenia od planu (C-54, odnotowane): `saveProviderToContacts` w `actions/parts/providers.ts`
(barrel `services.ts` nie może nieść akcji); przełącznik prognozy w `WatchersPanel` zamiast
nieistniejących „ustawień Pogody"; MyRequestsPage bez przycisku kontaktu (RequestDTO nie niesie
`providerId`). Wpis do `doświadczenia.md`: seed LLM w `migrate.js` (klucz złożony od 034,
błąd ukryty w `catch`+`warn`).
