# Dodatek A.13 — Status wdrożeń (żywy)

> **Plik aktualizowany per‑zalecenie, PRZED każdym commitem.** Jedno źródło prawdy do wznawiania sesji
> po przerwie (np. brak tokenów) i do końcowego Raportu 1:1.
>
> **Jak wznowić sesję:** otwórz tę tabelę, weź pierwszą pozycję ze statusem 🟡/⬜ w zalecanej kolejności,
> zajrzyj do jej planu (Dodatek A.2–A.12) i rozdziału źródłowego, rób dalej tą samą pętlą.
>
> **Legenda:** ✅ ZROBIONE · 🟡 W TOKU · ⏸️ ODŁOŻONE (powód) · ⬜ TODO
>
> **Polityka weryfikacji partii:** zmiany dotykające `src/app/**` (trasy), komponentów,
> `next.config`/migracji → pełny `./node_modules/.bin/next build`. Zmiany czysto logiczne
> (`src/actions/**`, `src/lib/**`, testy) → `tsc --noEmit` (to krok typów z buildu) + `npm run test:unit`.
> Zawsze: `check:actions` + `check:migrations`. Testy DB‑gated odpalają się z `DATABASE_URL` (lokalnie + CI z Postgresem).

## Postęp ogólny
| Priorytet | Razem | ✅ | 🟡 | ⏸️ | ⬜ |
|---|:---:|:---:|:---:|:---:|:---:|
| P0 | 22 | 22 | 0 | 0 | 0 |
| P1 | 129 | ~33 | ~6 | — | ~90 |
| P2 | 95 | ~6 | 0 | — | ~89 |

> **Uwaga (2026-06-23):** ta tabela zbiorcza dryfowała (utknęła na P1=7) — przez kilka sesji
> aktualizowałem tylko stopkę i wiersze obszarowe. Liczby są przybliżone — **źródło prawdy to
> wiersze obszarowe poniżej + sekcja „slice'ów"**. ✅ P1 obejmuje m.in. dwa duże refaktory rdzenia
> (Z-010 egzekutor AI, Z-213/361 `services.ts`). ⏸️ decyzje właściciela są zliczane osobno w
> `62-decyzje-wlasciciela.md` (12 pozycji 🔓). P2 ✅ = kilka domknięte „przy okazji" testami
> (Z-272/281/303/411/312). **Zostaje ~90 P1 + ~89 P2 + integracje/strategia/marketing (całe obszary ⬜).**

---

## P0 — w zalecanej kolejności realizacji (rozdz. 47)

### Brama 1 — prawno‑bezpieczeństwowa
| ID | Nakł. | Status | Data | Pliki / commit | Notatka |
|---|:--:|:--:|---|---|---|
| Z-052 | M | ✅ | 2026-06-16 | `src/actions/tasks.ts` | Audyt 57 plików akcji (skan plik+funkcja). 9/10 podejrzanych = false‑positive (auth+`userId`, `hasPermission(ADMIN)`, `assertCanEditSkin`). Realna luka: zadania `projectId=null` omijały guard → helper `assertTaskAccess` (projekt LUB twórca/przypisany) + naprawiony `reorderTask`. Build‑guard statyczny odrzucony (za dużo FP) — regresję pilnują testy Z‑172. |
| Z-190 | M | ✅ | 2026-06-16 | `src/actions/tasks.ts` | Spot‑check stron odczytu pieniądze/PII/cross‑user (portfel/health/notes/tasks/usługi): izolacja OK (`ownershipFilter`/`ownedByWhere` na liście, `assert*Access(parentId)` na dzieciach). Dziura Tasks (wspólna z Z‑052) domknięta. |
| Z-050 | M | ✅ | 2026-06-17 | `src/actions/privacy.ts`, `src/components/settings/PrivacySettings.tsx`, `src/app/settings/page.tsx` | `exportMyData()` zbiera komplet danych usera ze wszystkich modułów (+ kluczowe dzieci), bez danych zespołów i bez tokenów auth; sekcja „Prywatność i dane" w /settings → pobranie JSON. **Uzupełnienie 2026-06-17:** dołożone brakujące rekordy user-owned — `HealthSettings` (aiOptIn) i **`UserConsent`** (rejestr zgód RODO: regulamin/polityka) → kompletny eksport; oba mają FK CASCADE, więc purge (Z-051) je czyści — zweryfikowane asercją w `purge.test`. |
| Z-051 | M | ✅ | 2026-06-17 | `src/lib/privacy/purge.ts`, `src/actions/privacy.ts`, `src/components/settings/PrivacySettings.tsx` | `purgeUserData` (transakcja sterowana realnymi regułami FK: RESTRICT→usuń, SET-NULL→skasuj jawnie po właścicielu, reszta CASCADE) + `deleteMyAccount` (potwierdzenie e-mailem, blokada gdy właściciel zespołu, signOut/JWT). Zweryfikowane lokalnym testem (izolacja zachowana). **Aktualizacja 2026-06-17 (Z-370):** wykryto kolumny właściciela BEZ FK do User — `Contact.ownerId` i `ServiceFavorite.userId` — które analiza po regułach FK pominęła; bez jawnego `deleteMany` zostawały OSIEROCONE po usunięciu konta (RODO!). Dodano jawne kasowanie + asercja w teście. Decyzja przekazania zespołów = przyszłe P1. |
| Z-172 | M | ✅ | 2026-06-16 | `src/__tests__/isolation.test.ts`, `src/lib/tasks/access.ts` | Testy BOLA/IDOR: guardy `assertListAccess/ProjectAccess/RecipeAccess/PetAccess`, `assertTaskAccess` (w tym osobiste `projectId=null`) i `ownedByWhere` odrzucają obcego właściciela. DB‑gated (skip bez `DATABASE_URL` → `test:unit` zielony bez bazy; CI z Postgresem je odpala). `mock.module` niedostępne, więc test na poziomie guardów (nie mock auth). |
| Z-173 | M | ✅ | 2026-06-16 | `src/__tests__/services-marketplace.test.ts`, `src/lib/services/access.ts`, `src/lib/services/payment.ts` | `loadRequestAccess` (izolacja klient/wykonawca/odrzuć) + `netAmount` wyciągnięte do lib i przetestowane (DB‑gated + pure). Guard, na którym stoją markPaymentPaid/bookClientExpense/sendQuote/respondToQuote/openDispute. Idempotencja PAID i admin‑gate resolveDispute — przegląd kodu. |
| Z-053 | S | ✅ | 2026-06-16 | migracja `0185_user_consent`, `src/lib/legal/documents.ts`, `src/actions/legal.ts`, `src/app/legal/*`, `src/components/legal/ConsentBanner.tsx`, AppShell | Mechanika zgód gotowa: model `UserConsent` (wersjonowane), akcje accept/outstanding, strony `/legal/[key]`, rejestr podprocesorów (faktyczny: Google/Groq/Neon/Render), baner zgód w AppShell, link w Ustawieniach. ⏸️ TREŚĆ PRAWNA polityki/regulaminu = wersja robocza, wymaga prawnika/DPO. |

### Brama 2 — operacyjna
| ID | Nakł. | Status | Data | Pliki / commit | Notatka |
|---|:--:|:--:|---|---|---|
| Z-030 | S | ✅ | 2026-06-16 | `prisma/schema.prisma`, migracja `0186_owner_indexes` | Dodano brakujące `@@index(ownerId/ownerTeamId)` (Team, ShoppingList, Note, TaskProject, Store, PetSale). Migracja idempotentna (`IF NOT EXISTS`) — naprawiła też dryf schema↔DB (Note miał indeks w bazie bez `@@index`). 9/9 zweryfikowane. Reszta modeli już miała indeksy. |
| Z-341 | S | ✅ | 2026-06-16 | `prisma/schema.prisma`, `src/lib/pagination.ts` | Indeksy magazynu zweryfikowane (StorageItem: owner/team/warehouse/sku/barcode/supplier; StorageMovement: [itemId,createdAt] wspiera keyset). Helper keyset gotowy do wpięcia w listy ruchu/pozycji przy wzroście wolumenu. |
| Z-070 | M | ✅ | 2026-06-16 | `src/lib/pagination.ts` (+test), `src/actions/access.ts`, `src/components/admin/AuditLogPage.tsx` | Reużywalny helper keyset (cursor po id, sort [createdAt desc, id desc], keysetQuery/keysetResult + 6 testów). Wpięty end-to-end w log audytu (zamiast take:200) + przycisk „Załaduj starsze". Rollout na notes/tasks/news = ten sam helper (follow-up z Z-071). |
| Z-111 | S | ✅ | 2026-06-16 | `src/app/error.tsx`, `src/app/global-error.tsx`, `src/app/not-found.tsx`, `src/components/ui/ErrorState.tsx`, `src/lib/observability/report.ts` | Granice błędu: segmentowa (`error.tsx`+reset), globalna (`global-error.tsx` z własnym html/body), spójny `ErrorState`, strona 404. Błędy → `reportClientError` (seam pod Sentry/Z-090). |
| Z-090 | S | ✅ | 2026-06-16 | `src/app/api/health/route.ts`, `src/middleware.ts`, `src/instrumentation.ts`, `next.config.mjs`, `src/lib/observability/report.ts` | Część kodowa: publiczny `/api/health` (200/503 + ping DB, zweryfikowany), `instrumentation.ts` (unhandledRejection→seam, punkt initu Sentry), seam `reportClient/ServerError` Sentry‑ready. ⏸️ DSN Sentry + zewn. uptime-monitor + alert 5xx = konfiguracja właściciela (instrukcja w instrumentation.ts). |
| Z-171 | S | ✅ | 2026-06-16 | `src/__tests__/isolation.test.ts` | Alias `@/` działa w runnerze (tsx ^4.19 czyta tsconfig paths) — zweryfikowane; nowe testy importują przez `@/…`, więc regresja aliasu wywali suite. |
| Z-170 | S | ✅ | 2026-06-16 | `.github/workflows/ci.yml`, `package.json` (skrypt `typecheck`) | Job `verify`: npm ci → migrate deploy (Postgres service) → check:actions → check:migrations → typecheck → test:unit (DB-gated odpalają się) → next build (bez migrate.js). Bramka na PR/push do develop/master. Node 22 (glob `node --test` wymaga ≥22). Run #1 złapał błąd Node 20 → naprawione. |
| Z-430 | S | ✅ | 2026-06-16 | `.github/workflows/ci.yml` | Job `e2e-smoke`: Postgres + seed + `playwright install chromium` + `playwright test e2e/specs/smoke.spec.ts` (auto-start aplikacji przez webServer, E2E_TEST_MODE=1) + artefakt raportu. Run przeglądarki niewykonalny w sandboxie. Job `continue-on-error` (nieblokujący) do pierwszej walidacji smoke na runnerze. |

### Brama 3 — kosztowa AI
| ID | Nakł. | Status | Data | Pliki / commit | Notatka |
|---|:--:|:--:|---|---|---|
| Z-130 | M | ✅ | 2026-06-16 | migracja `0187_ai_usage`, `src/lib/ai/usage.ts`, `src/app/api/llm/home/agent/route.ts` | Trwały dzienny budżet AI per user w tabeli `AiUsage` (zapytania+tokeny; wspólna baza→działa między instancjami). `checkAiBudget` przed runem (429 z Retry-After do północy UTC), `recordAiUsage(meta.tokens)` w `finally`. In-memory limiter zostaje jako bezpiecznik anty-burst. Budżet wpięty w briefing oraz CENTRALNIE w `chatComplete` (opcjonalne `userId` → check+record; cache-hit darmowy) i włączony w drogie trasy: vision/OCR (ocr-image/text, magazyn scan/document) + generacyjne (generate-recipe, plan-week, order-draft, pets/insights, magazyn/insights). Tanie klasyfikacje (categorize/extract/search/enrich/parse-ingredients) też wpięte. Pozostają tylko trasy bez `session` w zasięgu (np. import-url — helper). |
| Z-511 | M | ✅ | 2026-06-16 | `src/lib/ai/usage.ts` (PLAN_LIMITS), `src/lib/ai/cache.ts`, `src/lib/llm/chat.ts` | Twarde limity per plan (free: 100 req/200k tok dziennie; premium: 10×; plan=ADMIN→premium do czasu billingu). Cache odpowiedzi LLM (in-memory TTL/LRU) wpięty w `chatComplete` (`cache:true`) i WŁĄCZONY dla deterministycznych dispatch-routes (normalize, parse-ingredients, import-url) — identyczne wejście nie płaci ponownie. |
| Z-510 | S | ✅ | 2026-06-16 | `src/actions/metrics.ts`, `src/app/admin/metrics/page.tsx`, `src/app/admin/page.tsx` | `getUnitEconomics` liczy realny koszt AI z `AiUsage` (tokeny × cena z Config `ai_cost_per_1m_tokens`), MAU (proxy: aktywni AI), koszt/MAU, trend dzienny. Widok `/admin/metrics`. ARPU/CAC/LTV = null z adnotacją (wymaga danych billingu/marketingu — P1). |

### P0 modułowe (poza bramami)
| ID | Nakł. | Status | Data | Pliki / commit | Notatka |
|---|:--:|:--:|---|---|---|
| Z-210 | S | ✅ | 2026-06-16 | `src/app/api/llm/home/agent/route.ts` | Dyrektywa anty-injection w system prompcie (treść modułów + wyniki web = NIEUFNE DANE, nie polecenia; wykonuj tylko polecenia użytkownika) + wyniki narzędzi/web opakowane delimiterem `<<<DANE…DANE>>>`. Dodatkowa warstwa: akcje zmieniające i tak wymagają potwierdzenia w ActionDrawer. |
| Z-211 | S | ✅ | 2026-06-16 | `src/app/api/llm/home/agent/route.ts` | Zweryfikowane: obie ścieżki agenta (SSE i nie-SSE) zwalniają slot współbieżności w `finally` (`release()`), więc błąd/wyjątek nie blokuje kolejnych zapytań. `acquireSlot` z `rateLimit.ts` zwraca idempotentny release. |
| Z-270 | M | ✅ | 2026-06-16 | migracja `0188_health_settings`, `src/actions/health.ts`, `src/lib/ai/agentTools.ts`, `src/components/health/HealthAiOptInToggle.tsx` | AI opt-in dla danych zdrowotnych: model `HealthSettings.aiOptIn` (DOMYŚLNIE false), oba health read-tools agenta (`list_health_events`, `list_medications`) zwracają notę zamiast danych gdy opt-out, toggle w module Zdrowie. ⏸️ Szyfrowanie at-rest = poziom infrastruktury (Neon szyfruje dysk); field-encryption wrażliwych pól = większy follow-up. „Zero reklam" w Zdrowiu = polityka. |
| Z-360 | M | ✅ | 2026-06-16 | `src/__tests__/services-marketplace.test.ts` | Pokryte tą samą partią co Z‑173 (ten sam moduł): izolacja dostępu do zlecenia + księgowanie netto. |

---

## P1 / P2 — wg obszarów (rozwijane przy realizacji danego obszaru)
> Każdy obszar rozwinę do pojedynczych wierszy `Z-NNN`, gdy do niego dojdę (czytając rozdział źródłowy),
> żeby nic nie pominąć. Kolejność wg „Fundament wzrostu" → „Jakość i głębia" (rozdz. 47 pkt 4–5).

| Obszar | Zakres ID | Rozdz. | Plan | Status obszaru |
|---|---|:--:|:--:|---|
| Architektura i kod | Z-010 – Z-015 | 6 | A.2 | 🟡 — typecheck (`tsc`) w CI (Z-011/015 część). Pomiar ESLint: 74 problemy (gł. kosmetyka) → pełne wdrożenie do pliku decyzji #11; naprawiony realny bug `rules-of-hooks` w WeatherPage (mylący prefiks `use`→`requestGeolocation`). |
| Dane / Prisma / skala bazy | Z-031 – Z-037 | 7 | A.2 | 🟡 — Z-030 ✅ (P0); **Z-031 ✅** (migracja `0192_query_indexes`: `Task` i `Item` nie miały ŻADNEGO indeksu → dodane `Task(projectId,parentTaskId)`/`(parentTaskId)`/`(createdById)`/`(assigneeId)`, `TaskComment(taskId)`/`(userId)`, `TaskShare(taskId)`/`(userId)`, `Item(listId,status)`; drift-check czysty, build+161 testów zielone). Z-032 (pooler Neon)=infra/decyzja; Z-033/036 (jawne onDelete na ~108 FK)=duża migracja FK, follow-up; Z-034/035/037=P2. |
| Bezpieczeństwo / RBAC / RODO | Z-054 – Z-059 | 8 | A.3 | 🟡 — Z-057 ✅ (testy renderera markdown + XSS: `javascript:`/`data:` blok, allowlista schematów, escape `"`). Z-050/051/052/053 w P0. **Z-059 ✅** (retencja kosza: czysta `trashCutoff` + globalny `purgeExpiredTrash` (wszyscy userzy — wcześniej inline-cleanup zwalniał tylko aktywnego usera, konta nieaktywne nigdy) + endpoint `/api/cron/retention` wyzwalany zewnętrznie [Bearer `CRON_SECRET` lub admin], 3 testy granicy). Retencja logów audytu = celowo zostają (compliance). **Z-054 ✅** (procedura sekretów udokumentowana w runbooku: sekret stały w env Render, rotacja=re-szyfrowanie inaczej utrata kluczy; `isSecretConfigured()` + check w `/admin/health` (czerwony gdy niebezpieczny fallback) + 6 testów `secrets.test`). **Z-058** (2FA) = decyzja właściciela (plik decyzji #9). **Z-055/Z-137 ✅** (minimalizacja danych wrażliwych w promptach: `list_wallet` agenta gated `FinanceSettings.aiAccessEnabled` (opt-out, domyślnie wł.) + toggle „Dostęp AI do danych finansowych" w Portfel→Ustawienia + migracja `0193`; zdrowie już gated od Z-270). |
| Wydajność / skalowalność | Z-071 – Z-083 | 9 | A.4 | 🟡 — Z-070 ✅ (P0, keyset); **Z-073 ✅** (eliminacja N+1: `getTasksForProjects` — N× `assertProjectAccess` → 1 zapytanie filtrujące po tej samej regule dostępu; `bulkSetStorageQuantities` (spis magazynu) — N× `findUnique` → 1 `findMany`+mapa. Kalendarz już zoptymalizowany — `Promise.all` + nested `select`). Z-072 (cache `unstable_cache`)=follow-up; Z-071 (wirtualizacja, `@tanstack/virtual`)=nowa zależność; Z-075/094 (płatny tier)=decyzja kosztowa; Z-082/083=plan/P2. |
| DevOps / CI/CD / koszty | Z-091 – Z-097 | 10 | A.5 | 🟡 — Z-090 ✅ (P0, seam Sentry + `/api/health`); **Z-091 ✅** (CI `verify` na PR+push: typy/testy/strażniki/build, Postgres jako usługa, Node 22; dołożony trigger gałęzi **`v2`** — bo tam teraz integrujemy); **Z-092 🟡** (build artefaktu jest DB-free — CI robi `next build` bez `migrate.js`; runbook granicy build↔migracja + rollback kodu vs migracji = `docs/devops/runbook-deploy-rollback.md`; pełne odsprzęgnięcie w prod = release command Rendera, infra); **Z-093 🟡** (runbook DR: restore z PITR/branch Neona + checklist; włączenie PITR i przećwiczenie restore + RPO/RTO = decyzja/infra ⏸️). **Z-096 ✅** (logi strukturalne: `lib/observability/log.ts` — `buildLogRecord`/`logEvent` (JSON jednoliniowy) + `timed()` do pomiaru czasu operacji; wpięte w endpoint retencji; 4 testy). Z-095 (smoke po deployu), Z-097 (alert kosztów) = follow-up. |
| UX / design system / a11y / i18n | Z-110 – Z-118 | 11 | A.6 | 🟡 — Z-111 ✅ (P0, error.tsx/ErrorState); **Z-110 ✅** (dostępny prymityw `ui/Modal` na Radix Dialog — pułapka focusu, role/aria-modal, aria-labelledby, Esc, blokada scrolla, przywrócenie focusu; eksport w `ui/index.ts`); **Z-114 🟡** (modal Pets deleguje do `ui/Modal` → wszystkie modale Pets dostały a11y bez zmian w wywołaniach; `<img>` w realnych komponentach mają `alt` — 3 trafienia bez `alt` to generatory ikon `ImageResponse`, nie DOM; migracja pozostałych ~30 modali ad-hoc do prymitywu = follow-up); **Z-115 częściowo** (`formatMoney` już na `Intl.NumberFormat`; warstwa `t()`/i18n = przyrostowy follow-up). Z-112/113 (jeden EmptyState — 2 warianty o różnym API), Z-116/117/118 = follow-up/P2. |
| AI / LLM | Z-131 – Z-138 | 12 | A.7 | 🟡 — Z-130 ✅ (P0); **Z-132 ✅** (cache odpowiedzi LLM `chatComplete({cache:true})` wpięty w 3 operacje deterministyczne: normalize, parse-ingredients, import-url; in-memory LRU+TTL); **Z-133 ✅** (`resolveLlmChain` [model admina → fallback Groq, deduplikacja] + `chatComplete`/`chatStream` przechodzą łańcuch, przełączając się przy 429/5xx/sieci; completery zwracają realny status + łapią błąd sieci → 503; `isRetryableLlmStatus` z testem). Z-134 (tańszy `dispatch`)=konfig admina; Z-131/074 (kolejka Job)=większy follow-up; Z-135/136 (monitoring/eval)=follow-up. **Z-137 ✅** (=Z-055: minimalizacja danych w promptach — finanse opt-out + zdrowie opt-in). |
| Integracje | Z-150 – Z-158 | 13 | A.8 | 🟡 — **Z-150 ✅** (feed iCal: `collectCalendarEvents(userId)` wyodrębniony do `lib/calendar/collect.ts`; czysty `buildICalendar` (`lib/calendar/ical.ts`, RFC 5545, +7 testów); odwoływalny `User.icalToken` (migracja `0195`, `@unique`); route `/api/calendar/ical?token=` (poza middleware, scoping po userze — brak przecieku) zwraca okno −1…+2 mies.; akcje `getMyIcalFeedUrl`/`regenerateIcalFeed`; karta subskrypcji w /settings). **Z-153 ✅** (=Z-300, import CSV Portfela). Z-151 (odczyt Google Calendar) = decyzja #5 (scope OAuth + review Google). **Z-157 ✅** (ujedn. warstwa integracji: `lib/integrations/resilientFetch.ts` — `resilientFetch` [timeout per próba + retry/backoff na 429/5xx/sieć, `fetchImpl`/`sleep` wstrzykiwalne] + `fetchJsonSafe` [miękka degradacja→null]; **9 testów** deterministycznych; wpięte w integracje tła/latency-tolerant: **openMeteo** [geocode+forecast] + **news** [article, rss, webSearch Brave+DDG]. Świadomie POZA: ors/overpass [user-facing, timeout 20–25 s → retry mnożyłby latencję] i groqVision [LLM — własny retry przez `resolveLlmChain` Z-133]). Z-156 (Gmail) = follow-up; Z-152/154/155/158 = P2. |
| Testowanie / jakość | Z-174 – Z-188 | 14 | A.9 | 🟡 — testy DB-gated (izolacja/marketplace) + 7 markdown(XSS) + 9 skórek(CSS-inj) + 6 paginacji + 5 userTime(strefa/doba — **wykryły i naprawiły bug ~1 s w `end` doby**); + 6 currency(waluty) + 6 habitStats(serie) + 5 storeRoute(TSP) + 6 recurrence-edges + 4 storeLayout + 6 categorize + 6 ai-budget + 3 purge + 6 autoExpense + 4 keyset-integration + 3 permissions(RBAC) + 6 medicationSchedule + 7 petGenetics + 3 cache + 3 rateLimit + 4 petWelfare(alarmy terrariów) + 6 secrets(Z-054) + 3 trash(retencja Z-059) + 1 fallback(Z-133); **Z-176 ✅** (7 testów czystej logiki self-lockout admina `countDistinctAdminHolders` — symulacja excludeRoleGrant/removeUserRole, ostatni admin); **Z-181 ✅** (5 testów kontraktu read-toolów agenta + regresja bramek prywatności Z-055/Z-270: finanse opt-out, zdrowie opt-in, izolacja). **+ 8 news/parseRss** (RSS 2.0 + Atom: tytuł/link/data/opis, strip tagów, obcięcie 600, pomijanie pozycji bez linku, śmieci→[]) — parser ingest newsów był nietestowany; **+ 11 news/article** (extractText: wycięcie script/style, encje, preferencja <article>, limit; metaContent: oba porządki atrybutów; extractPublishedAt: meta/JSON-LD/<time>). suite **245 testów (z DB-gated, Postgres lokalny)**, CI je odpala. Z-170/171/172/173 w P0; Z-178/179 zaliczone. |
| Współdzielenie / rodziny | Z-191 – Z-198 | 15 | A.12 | 🟡 — Z-190 ✅ (P0); Z-191 ✅ (team-awareness zweryfikowane: Shopping/Tasks/Notes/Kitchen/Pets/Health/Habits/Flota/Portfel/Languages/Storage/Workshop/Contacts mają `ownerTeamId`; Store/News/Weather/ProjectGroup celowo user-only); **Z-192 ✅** (preset „household": `Team.kind`, UI „Gospodarstwo domowe", **domyślne wspólne kompletne — lista zakupów + projekt zadań + budżet domowy** (`WalletElement` team-owned); kalendarz = agregat team-aware; zaproszenia przez `invitations`; badge). **Z-195 ✅** (onboarding rodziny: karta „Pierwsze kroki" na stronie household — checklist utworzenia, podpowiedź zaproszenia domowników [stan z liczby członków], deep-linki do domyślnych wspólnych zasobów zakupy/zadania/budżet; akcja `getHouseholdOnboarding` z kontrolą członkostwa). Pozostaje: role rodzic/dziecko **Z-194** + ujednolicony „Udostępnij" **Z-193** (osobne P1) |
| Audyt modułów | Z-210 – Z-419 | 16–41 | wg obszaru | 🟡 (P0: Z-210/211/270/341/360/430 ✅). **Zakupy** zweryfikowane: Z-220 (modal tworzenia listy) ✅ już jest, Z-224 (archiwizacja/„zakończ zakupy") ✅ już jest; Z-221 (DnD pozycji) → decyzja #12 (kolizja z sortem po trasie); Z-222/223/225 = P2/M. Reszta modułów do weryfikacji per chapter. **Zaliczone testami tej sesji:** Z-272 (dawkowanie recurrence+strefa = medicationSchedule.test) ✅, Z-281 (habitStats serie/doba) ✅, Z-303 (currency toBase + alias) ✅, Z-411 (sanitizacja tokenów CSS-inj = skins.test) ✅, **Z-312 ✅** (testy brzegowe SRS: granica grade=3, reset serii po lapse, długa przerwa liczy dueAt od teraz, kumulacja lapses + podłoga ease — 4 nowe testy). **Kontakty:** Z-370 ✅ (Kontakty w RODO: wykryto i naprawiono brak kasowania `Contact`/`ServiceFavorite` przy usuwaniu konta — kolumny właściciela bez FK; patrz Z-051). **Przypomnienia (silnik powiadomień `syncReminders`):** Z-230 (terminy zadań) ✅, Z-271 (leki/wizyty) ✅, Z-290 (przegląd/OC/serwis floty) ✅, Z-310 (powtórki SRS) ✅ — już wpięte; **Z-280 ✅ dorobione** (nawyki zaplanowane na dziś i nieodhaczone: tryb dni `isScheduledOn` / tryb celu tyg. `weekDoneCount<goal`, dedupe per dzień). **Z-241 ✅ zweryfikowane** (autoryzacja + limity załączników: notatki/zdrowie/flota mają `assert*Access` + walidację schematu (`data:`/`http`) + limit ~2,5 MB; `/api/drive/upload` ma auth + limit 10 MB + kontrolę typu `File`). **Z-215 ✅** (`getAiConversation` ładuje teraz okno `MESSAGE_WINDOW=300` najnowszych wiadomości zamiast wszystkich; `appendAiMessage` ogranicza sidecar `data` do 128 KB przez `boundMessageData` — 6 testów). **Z-218 ✅** (walidacja kluczy sekcji pulpitu: wspólny whitelist `DASHBOARD_SECTIONS` + `sanitizeSectionKeys` (whitelist+dedup) używany i przez `HomePage`, i przez `dashboardPrefs` przy zapisie/odczycie — literówka w JSON nie tworzy martwej pozycji; 5 testów). **Flota:** Z-291 ✅ (TCO pojazdu: `computeVehicleTCO` — paliwo+serwis (w tym OC/AC) + koszt/km z odometrów; wyświetlone w karcie pojazdu; 6 testów). **Portfel:** Z-300 ✅ (import wyciągu CSV: `parseBankCsv` — autodetekcja separatora/kolumn, daty dd.mm.yyyy/ISO, kwoty z przecinkiem/nawiasem księgowym; `importBankCsv` idempotentny po `sourceId` z hasza treści; upload→**podgląd transakcji (modal a11y) z potwierdzeniem przed zaksięgowaniem**→wynik na stronie elementu; 6 testów parsera). **Z-252 ✅** (food cost: dodano `RecipeIngredient.unitPrice` (migracja 0194, kolumna nullable), czysty `computeRecipeCost` — koszt przepisu + koszt/porcja, składniki bez ceny pomijane z flagą „częściowy", opcjonalne nie psują kompletności; 7 testów; input ceny w edytorze przepisu + wyświetlenie kosztu/porcji w widoku; pole podpięte we wszystkich 4 ścieżkach create) + **koszt tygodnia planu posiłków** (`getMealPlanCost` — osobne lekkie zapytanie, bez obciążania gorących ścieżek home/kalendarz; badge „~X zł/tydz." w nagłówku planu, gated do załadowanego tygodnia bo nawigacja jest klient-side). **Hodowla:** Z-262 ✅ częściowo (przychód ze sprzedaży potomstwa per para hodowlana — `getPetBreeding` wzbogaca pary o `revenue`/`soldCount`, potomstwo wg `sireId=maleId & damId=femaleId`, 2 zapytania bez N+1, wyświetlone w karcie pary; ⏸️ strona kosztowa ROI „− koszty z Portfela" wymaga modelu atrybucji kosztów do pary). **Pogoda:** Z-330 ✅ (cache porad LLM `chatComplete({cache})` per lokalizacja/dzień/pora/prognoza — zero powtórnych tokenów; też ocena watcherów; Z-331 częściowo = resilientFetch w openMeteo z Z-157). **Z-232 ✅** (spójność skrótów `j/k/x/e/d` — rollout huba `useKeyboardShortcuts` na Kontakty/Nawyki/Dostawcy/Zdrowie/Leki/Przepisy; Notatki/Zadania/Zakupy miały wcześniej; + onEnter→nawigacyjne Języki/Zwierzęta/Flota/Portfel/Warsztaty (j/k+Enter) + Magazyn partial (j/k+Enter/e+a+/) — Z-232 ZAKOŃCZONE, patrz nota na końcu pliku). |
| Model biznesowy / monetyzacja | Z-470 – Z-476 | 42 | A.10 | 🟡 — Z-471 ✅ (`Subscription` + `lib/plans.ts` getActivePlan/hasFeature + sekcja „Twój plan"; AI-budżet czyta plan); Z-472=Z-130 ✅; Z-510 ✅. ⏸️ Z-473 bramka płatności + Z-470 linia free/premium = decyzja właściciela; Z-474 reklamy=P2; Z-476 prowizja marketplace=TODO |
| Strategia podaplikacji | Z-490 – Z-495 | 43 | A.11 | ⬜ |
| Model ilościowy | Z-512 – Z-515 | 44 | A.10 | ⬜ (Z-510/511 w P0) |
| Marketing | Z-530 – Z-535 | 45 | — | ⬜ |

---

**Bug-fix (Kitchen plan):** nawigacja tygodni planu posiłków była klient-side (`setAnchorDate` bez zmiany URL) → inne tygodnie puste, posiłki dodane poza bieżącym tygodniem znikały po rewalidacji. Naprawione na URL-driven (`router.push ?week=`, serwer przeładowuje wpisy+koszt; `useEffect` resync kotwicy). Test e2e `scenario-kitchen-plan-week-nav` (URL zmienia `?week=`). Patrz doświadczenia.md.

**Z-010 (w toku, slice 1/N):** rozbicie egzekutora AI (`execute/route.ts`, było 1467 linii). Enabler: `check-action-coverage.js` skanuje teraz też `src/lib/ai/executors/*.ts` (check podąża za przeniesionymi `type === "..."`). Slice 1: handler Petów → `src/lib/ai/executors/petExecutor.ts` (route 1467→1228, −239 linii). Check zielony (95 akcji, w tym pety z nowego pliku). Kolejne slice'y: storage/health/kitchen/flota/portfel/languages/news/weather.

**Z-010 slice 2/N:** wspólna infrastruktura egzekutora (typy `ActionResult`/`ExecOutcome` + helpery `asStr`/`undoAction`/resolvery rekordów) → `src/lib/ai/executors/shared.ts`. Route `execute/route.ts` **1228→962 linii** (slice 1+2: 1467→962, −505). Klienci (`ActionDrawer`, `AICommandSheet`) importują `ActionResult` z shared. Czyste przeniesienie (tsc/check/build zielone; baseline testów bez zmian — 184 pass/7 env-skip). Kolejne slice'y: przenoszenie domen (languages/news/weather/storage/health/kitchen/flota/portfel) — bloki rozproszone w `executeAction`, wymagają konsolidacji.

**Z-010 slice 3/N:** handler modułu Zdrowie (wizyty/badania + leki/pielęgnacja, 7 akcji — blok jednomiejscowy, samodzielny) → `executors/healthExecutor.ts`. Route **962→890**. Slice 1+3 razem: 1467→890 (−577, ~39%). Usunięte z route'a importy zdrowia/leków + resolvery (`resolveHealthEventId`/`resolveMedicationId` używane już tylko przez healthExecutor) + typy `HealthKind`/`HealthStatus`. check:actions zielony (akcje zdrowia wykryte z nowego pliku), baseline testów bez zmian.

**Z-010 slice 4/N:** domeny rozproszone (po 2 grupy `module === "..."` scalone w jeden executor; drugą grupę kasujemy — `check:actions` pilnuje pokrycia): **languages** (6 akcji), **news** (4), **weather** (5) → `languageExecutor.ts`/`newsExecutor.ts`/`weatherExecutor.ts`. Route **890→759**. Łącznie slice 1-4: 1467→759 (−708, ~48%). 5 executorów + shared.

**Z-010 slice 5/N:** **warsztaty** (2 akcje) + **reports** (save_report) → `warsztatExecutor.ts`/`reportExecutor.ts`. Route **759→723**. Łącznie slice 1-5: 1467→723 (−744, ~51%). 7 executorów + shared. Zostaje: shopping/tasks/notes (2 grupy), habits/portfel/kitchen/flota (3 grupy), magazynowanie (2).

**Z-010 slice 6/N:** **notes** (5 akcji: CRUD + toggle_pin) + **shopping** (10 akcji: pozycje + listy) → `notesExecutor.ts`/`shoppingExecutor.ts`. Route **723→579**. Łącznie slice 1-6: 1467→579 (−888, ~60%). 9 executorów + shared. Zostaje: tasks (2 grupy), habits/portfel/kitchen/flota (3 grupy), magazynowanie (2).

**Z-010 slice 7/N:** **tasks** (9 akcji: zadania + projekty) → `tasksExecutor.ts` (bierze currentProjectId). Route **579→454**.

**Z-010 ZAKOŃCZONE (slice 8/8):** ostatnie domeny team-scope (3 grupy każda) → executory: **habits** (5), **portfel** (7), **flota** (5), **kitchen** (9), **magazynowanie** (5). Usunięty wspólny `teamOr` (każdy executor liczy własny przez `ownerOrArr`) i wszystkie zbędne importy akcji/resolverów z route'a. **`executeAction` to teraz czysty dispatcher** (`if (module === "X") return executeXAction(...)`) + final throw + POST. Route **1467 → 148 linii (−90%)**. 15 executorów per-domena + `shared.ts`, wszystko pilnowane przez `check-action-coverage.js` (skanuje `executors/*`). tsc/check/build zielone, baseline testów niezmieniony.

**Z-213/361 (w toku, slice 1/N):** rozbicie `actions/services.ts` (było 1400 linii). Slice 1 (bezpieczny, bez dotykania granicy Server Action): plumbing (mappery DTO `toListingDTO`/`mapRequest`, `slugify`/`uniqueProviderSlug`, guardy `requireOwnProvider`/`requireMyProvider`, `computeSlots`, stałe) → `src/lib/services/helpers.ts`; typy `ListingSort`/`ProviderStats`/`ModerationDisputeDTO` → `src/lib/services.ts` (3 importery komponentów zaktualizowane). services.ts **1400→1229**. tsc/build/testy zielone. Brak e2e dla usług — split akcji per-obszar (slice 2+) weryfikowalny tylko build+typy (uwaga: moduł wdrożony).

**Z-213/361 slice 2/N:** akcje **spory/moderacja** → `src/actions/services/disputes.ts` (standalone `"use server"`). Ustalony wzorzec: `"use server"` NIE pozwala re-eksportować (build: „Only async functions… in use server file"), więc każdy obszar to samodzielny plik `"use server"`, a importery danego obszaru wskazują nowy path. services.ts **1229→1138**.

**Z-213/361 slice 3/N:** kolejne 5 obszarów → osobne pliki `actions/services/`: **promo** (6), **payments** (3), **favorites** (2), **stats** (1), **images** (2). Importery zaktualizowane (RequestThread, ProviderPanelPage, ProviderPublicPage, services/page, provider/page). services.ts **1138→845**. tsc/build/testy zielone. Zostaje: providers/categories, listings, requests, reviews/messaging/quotes, scheduling/staff/slots.

**Z-213/361 slice 4/N:** **scheduling** (dostępność + pracownicy M14 + sloty/rezerwacja, 9 akcji) → `actions/services/scheduling.ts`. Importery: AvailabilityEditor, StaffManager, MyRequestsPage, ListingDetailPage. Usunięty osierocony import `@/lib/serviceSlots` z services.ts. services.ts **845→706**. Zostaje: providers/categories, listings, requests/reviews/messaging/quotes.

**Z-213/361 ZAKOŃCZONE (slice 5/5):** ostatnie 4 obszary → `actions/services/`: **providers** (kategorie+profil+weryfikacja, 6), **listings** (5), **requests** (5), **messaging** (oceny+wątek+wyceny, 5). `actions/services.ts` przekształcony w **20-linijkowy barrel** (`export * from "./services/*"`, BEZ `"use server"`) re-eksportujący wszystkie 11 plików obszarów — publiczny import `@/actions/services` zachowany dla pozostałych konsumentów (zero ich dotykania). **services.ts 1400 → 20 linii.** 11 plików akcji per-obszar + `lib/services/helpers.ts` + typy w `lib/services.ts`. tsc/build/testy zielone (barrel `export *` nad plikami `"use server"` przechodzi build).

**Z-232 (spójność skrótów `j/k/x/e/d`) — rollout huba `useKeyboardShortcuts`:** Wpięte/zmigrowane: **Kontakty, Nawyki** (migracja z własnego listenera „shadow hub" + dołożone `d`), **Dostawcy, Zdrowie, Leki** (`j/k` po harmonogramach; odhaczanie dawek „na dziś" świadomie zostaje pod myszą — jeden `onToggleStatus` nie obsłuży dwóch list), **Przepisy** (migracja inline `/`+`n`, karty nawigacyjne). **Notatki/Zadania/Zakupy** miały hub wcześniej. Wzorzec: `focused` (number, −1) + `ordered[focused]` w rodzicu, akcje karty wyniesione z komponentu karty do rodzica, ring `var(--border-focus)`, `onMouseEnter`=focus; opcjonalny kontrakt huba pozwala pominąć search/filterTab/palette. **Listy nawigacyjne — DOKOŃCZONE** (decyzja właściciela „tak"): kontrakt huba rozszerzony o **`onEnter`** (Enter=„otwórz", z guardem: nie przejmuje Entera na realnej kontrolce button/a/select/[role=button]); **Języki, Zwierzęta, Flota, Portfel, Warsztaty** mają teraz `j/k` (fokus) + Enter (otwórz detal) + `a/n` (dodaj), kafelek=`<Link>` z ringiem od `focused` + `onMouseEnter`. **Magazyn** (`StorageList`) — **częściowy keyset**: `j/k` po spłaszczonej głównej liście (globalny indeks przez `Map(id→index)`), Enter/`e`=otwórz arkusz edycji, `a`=dodaj, `/`=szukaj; usuń/zmiana ilości zostają w arkuszu (nie in-place), sekcje „do uzupełnienia"/„terminy" pod myszą. Weryfikacja: `tsc --noEmit` czysty po każdej partii (zmiany czysto-komponentowe; pełny `next build` robi CI/Render na deployu `develop`). Lekcje w `doświadczenia.md` (shadow-huby; akcje-w-karcie/wielolistowość/dialog-guard; onEnter+nawigacyjne/Magazyn). **Z-232 = pełny rollout ZAKOŃCZONY** (proste listy + strony wielolistowe + nawigacyjne + Magazyn-partial).

**Sesja autonomiczna „dobijanie" (2026-06-24):** po skanie audytu (agent Explore) zweryfikowane przy realnym kodzie i ZROBIONE: **Z-442 ✅** (Truck: banner „funkcja eksperymentalna" — czysto UI). **Z-264 ✅** (RODO PetSale: model ma FK `onDelete:Cascade` do User i Pet → PII kupującego [`buyerName`/`buyerContact`] kasuje się wraz z kontem; dodany test regresji kaskady w `purge.test`). **Z-400 (audyt soft-delete) — WYNIK:** `recordTrash`/`TrashModule` (`lib/trash.ts`) pokrywa TYLKO `notes`+`tasks`; pozostałe moduły kasują twardo (mimo opisu „across modules" w CLAUDE.md). Pełne rozszerzenie Kosza = większy follow-up (restore per-moduł + decyzja, które encje odzyskiwalne) — NIE w tej sesji. **Testowanie/jakość +37 testów** (suite 256→293, lokalny Postgres): `polishPlural` (5), `petSpecies` (4), `formatMoney`+Portfel (4: ICU-odporne), `isoDay`/`monthRange` Kalendarza (2), **`ownership`/BOLA** (2: `ownedByWhere`+`assertOwnership`), `weather/presets` (4: granice `currentDayPart`), `kitchenDate` (5), **`petPresets`/gating** (5: `resolveFeatures` — nadpisania JSON / zły JSON / nieznane klucze), `omniaClipboard.buildOmniaPrompt` (3), `news/timeAgo` (2), `purge`/Z-264 (1). Suite **293 pass / 0 fail**. Odrzucone po weryfikacji (nieaktualne/zrobione/nieweryfikowalne): Z-251 (parser=LLM), Z-382 (kalendarz już zoptymalizowany), Z-460 (`logAudit` pokrywa config/access/llmConfig), Z-321/Z-432 (ścieżka offline / selektory e2e — nieweryfikowalne bez uruchomienia aplikacji/przeglądarki).

**Sesja modale + EmptyState + RODO c.d. (2026-06-24, gałąź robocza — bez deployu, limit Render):** **Z-112/113 ✅** (usunięto martwy duplikat `ui/EmptyState` — barrel `@/components/ui` nieużywany; żywy zostaje `ui/home/EmptyState`). **Z-114 🟡** (migracja modali ad-hoc na dostępny `ui/Modal` [Radix: focus-trap / aria-modal / aria-labelledby / Esc / blokada scrolla]): zmigrowane **ImportFromUrlDialog, ImportFromImageDialog, ImportFromAIDialog, CookbookEditDialog**; wzorzec = nagłówek→`title`, treść→`children`, przyciski→`footer`. Zostają (~20): kitchen-plan dialogi (PlanWeek/ShoppingFromPlan/SlotEditor/ShopForRecipe), sheety (Storage/Pantry edit), pickery (CategoryIcon/IconLibrary), modal „Nowy warsztat", modale osadzone w stronach (Tasks/Shopping/News/Weather/Magazyn). **Testy RODO/BOLA c.d.:** **Z-301 ✅** (finanse Portfela — kaskada przy usunięciu konta), **QA-anonimizacja ✅** (audyt `purge` kompletny — QaTestScenario słusznie anonimizowany, nie kasowany), **cookbook ✅** (ostatnia luka izolacji BOLA), **plans gating ✅** (fallback ≠ premium). **Suite 298/298** (lokalny Postgres).

_Ostatnia aktualizacja: 2026-06-24 - Modale+EmptyState+RODO: Z-112/113 ✅ + Z-114 🟡 (4 dialogi na ui/Modal) + Z-301/QA/cookbook/plans (suite 298). Wcześniej: Sesja autonomiczna „dobijanie": Z-442 (Truck banner) + Z-264 (RODO PetSale, test) ✅, audyt Z-400 (soft-delete=tylko notes/tasks), +15 testów (suite 272 z DB) → łącznie +37 (suite 293 z DB; m.in. ownership/BOLA, petPresets-gating, weather, kitchenDate, timeAgo). Wcześniej: Z-232 ZAKOŃCZONE: rollout huba ✅ (Zdrowie/Leki/Przepisy + Kontakty/Nawyki/Dostawcy) + onEnter→listy nawigacyjne (Zwierzęta/Flota/Portfel/Warsztaty/Języki, j/k+Enter) + Magazyn partial. Wcześniej: Testy: +8 news/parseRss (parser RSS/Atom nietestowany). Integracje Z-150/153/157 ✅. P1 ~37/129. Suite 245 pass / 0 fail (DB up).
